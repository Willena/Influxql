package io.github.willena.influxql.ast;

/**
 * A marker interface for all {@link Node} part of an expression (when selecting fields, in where condition, ...)
 */
public interface Expression extends Node {
}
